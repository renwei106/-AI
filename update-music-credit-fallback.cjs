const fs=require('fs');let s=fs.readFileSync('dist/v4.js','utf8');s=s.replace("$('.record-calibration').textContent='程序合成 · 纯音乐'","$('.record-calibration').textContent=TRACKS[trackIndex].author||'纯音乐'");s=s.replace("const syncBeforeDiscControls=syncMusicUI;syncMusicUI=function(){syncBeforeDiscControls();","const syncBeforeDiscControls=syncMusicUI;syncMusicUI=function(){syncBeforeDiscControls();const credit=$('.music-composition .record-calibration');if(credit)credit.textContent=TRACKS[trackIndex].author||'纯音乐';");fs.writeFileSync('dist/v4.js',s);
