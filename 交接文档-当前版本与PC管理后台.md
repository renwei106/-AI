# 拾隅当前版本与 PC 管理后台交接文档

更新时间：2026-09-13  
交接对象：打点  
当前前台预览：`http://127.0.0.1:4318/`

## 1. 项目总览

当前工作主要分成两条线：

1. 拾隅前台原型：用户实际看到的收藏空间、首页主题、会员弹窗、分享、音乐、时钟、全幕光影等交互都在这里迭代。
2. PC 管理后台：作为聚合入口和配置中枢，负责统一进入不同应用后台，并维护后台管理员、角色、应用入口与应用权限；后续会员、主题、应用开关、价格、活动规则等也应逐步迁移到后台配置。

前台产品用户和后台管理员是两个身份域。前台登录、会员状态、用户空间等属于产品用户侧；PC 管理后台登录、角色和应用授权属于后台管理员侧，不能混用 token 或权限。

## 2. 仓库情况

目前与本项目直接相关的仓库一共 4 个：

| 项目 | 本地路径 | Git 远端 | 本地端口 | 说明 |
| --- | --- | --- | --- | --- |
| 拾隅前台原型 / 当前版本 | `D:\系统文件\文档\ChatGPT\导航站` | `https://github.com/renwei106/-AI.git` | `4318` | 当前用户端原型，静态页面加本地分享服务 |
| 聚合管理后台 / PC 管理后台 | `D:\系统文件\文档\ChatGPT\聚合管理后台` | `https://github.com/renwei106/qing-platform-admin.git` | `5175` | 聚合入口、应用入口配置、管理员、角色与应用权限 |
| 轻时间管理后台 | `D:\系统文件\文档\ChatGPT\轻时间管理后台` | `https://github.com/renwei106/qing-time-admin.git` | `5176` | 轻时间业务后台，维护轻时间业务配置 |
| 轻间管理后台 | `D:\系统文件\文档\ChatGPT\轻间管理后台` | `https://github.com/renwei106/qing-space-admin.git` | `5177` | 轻应用/轻间业务后台，后续会员、工具、模型等配置主要落在这里 |

聚合管理后台的 README 里称为“三仓关系”，指的是三个后台仓库：聚合管理后台、轻时间管理后台、轻间管理后台。再加上当前用户端原型仓库，交接时按 4 个仓库理解最清楚。

## 3. 当前版本基础信息

当前前台项目是一个本地静态原型，主要文件在：

- `dist/index.html`：入口页面。
- `dist/v4.js`：当前大量业务交互和覆盖式迭代逻辑。
- `dist/v4.css`：当前视觉样式。
- `preview.cjs`：本地预览服务，监听 `http://127.0.0.1:4318/`。
- `share-server.cjs`：本地分享接口，包含分享创建、密码访问、有效期、一次性访问等模拟逻辑。
- `.local-shares/`：本地分享数据目录，不应作为正式数据源。
- `baselines/`、`checks/`、`verify-*.cjs`、`update-*.cjs`：迭代留痕、验证脚本和一次性修补脚本，很多是辅助开发产物，不等同于正式源码结构。

启动方式：

```powershell
node preview.cjs
```

访问地址：

```text
http://127.0.0.1:4318/
http://127.0.0.1:4318/?page=membership
```

当前 Git 状态提醒：

- 最近一次已完成提交：`0bf73a6 Save latest UI interactions and public sharing`
- 当前工作区还有 `dist/v4.js` 的未提交改动，主要是分享有效期里新增“阅后即焚（仅访问一次）”选项。
- 工作区还有大量未跟踪的基线目录和验证脚本，提交前需要人工筛选，避免把不需要的临时产物全部提交。

## 4. 当前前台功能状态

拾隅前台当前已经模拟了这些主要模块：

- 首页主题：黑胶唱日留白日常、黑胶唱片、宇宙漫游、全幕光影、人生影院、时光报纸、翻页时钟等。
- 主题切换：左上角主题菜单；封面页右上角拉绳式日间/夜间/跟随系统切换；空间页不显示拉绳入口。
- 空间与收藏：空间、场景、分组、网址管理；部分会员限制入口有会员标识和会员弹窗。
- 会员体系：会员触发弹窗、会员中心页面、套餐对比、支付方式模拟、协议弹窗、支付成功/失败模拟、邀请奖励弹窗、订单侧边栏模拟。
- 分享功能：分享入口对所有用户开放；密码和有效期属于会员功能；新增的“阅后即焚（仅访问一次）”是有效期里的正式选项，服务端用 `once: true` 模拟第一次成功访问后失效。
- 时光报纸：支持进入当前页面编辑、封面图上传和删除、保存/取消后恢复正常状态。
- 全幕光影：全屏幕布式自然风光背景，包含日夜、太阳/月亮运行和动态细节。
- 音乐主题：黑胶唱片点击切歌，中间圆圈播放/暂停，切碟动画和光晕效果已多轮调试。
- 宇宙漫游：太阳系放大初始状态，点击天体进入地平线介绍，月球和小行星带等细节已模拟。
- 翻页/时间主题：正在做弹性下落、碰撞、单位显示和地平线下沉等效果，属于偏实验性的高动效模块。

## 5. PC 管理后台注意事项

PC 管理后台不是某一个业务应用的后台，而是聚合入口和权限配置中心。接手时不要把它当成“所有业务逻辑都写在这里”的后台。

它当前主要负责：

- 应用入口配置：维护轻时间、轻间、轻应用等入口地址、名称、说明、启用状态。
- 后台管理员：维护后台登录账号。
- 角色与应用权限：给管理员配置可进入哪些应用。
- 公共后台 UI：`packages/admin-ui` 是后台公共框架来源，业务后台使用固定版本，不应跨仓库直接引用源码。
- 平台会话校验：本地联调中，业务后台通过平台会话和应用权限判断是否可进入。

它后续应该承接的配置：

- 会员套餐：免费版、月度会员、连续包月、年度会员、连续包年。
- 会员价格：例如月度 10 元、连续包月 8 元、年度 99 元、连续包年 79 元等，目前前台为模拟数据，正式应后台配置。
- 会员权益数量：空间数量、场景数量、分组数量、每分组网址数量等。
- 会员权益开关：空间独立配置、更多网址样式、分享密码、分享有效期、阅后即焚等。
- 协议配置：会员服务协议、连续包月服务协议、连续包年服务协议，名称与内容都应后台可配置。
- 邀请奖励：注册成功奖励天数、付费成功奖励天数、活动是否开放、邀请明细和奖励到账状态。
- 主题/应用开关：新增主题、主题排序、主题是否会员专享、应用是否展示、应用入口地址。
- 支付配置：支付宝、微信支付方式、默认支付方式、连续订阅协议关联、支付回调状态。

这些配置不建议长期写死在前台 `dist/v4.js`。前台现在只是为了演示交互和视觉，后续需要由后台配置接口驱动。

## 6. 三个后台的关系

聚合管理后台：

- 本地地址：`http://127.0.0.1:5175/`
- 演示账号：`admin / 123456`
- 职责：平台入口、应用配置、管理员、角色、权限。

轻时间管理后台：

- 本地地址：`http://127.0.0.1:5176/apps/qing-time/dashboard`
- 职责：轻时间业务配置、首页视觉、公开读取接口等。
- 注意：先启动平台后台，再启动轻时间后台。

轻间管理后台：

- 本地地址：`http://127.0.0.1:5177/tools`
- 职责：轻应用工具、用户、会员、模型配置等业务能力。
- 注意：会员配置、模型配置、工具配置后续更适合落到这里，再由聚合后台提供入口和权限。

统一启动方式在聚合管理后台中已有脚本：

```powershell
powershell -ExecutionPolicy Bypass -File .\start-admin.ps1
```

脚本固定使用 5175、5176、5177 端口，端口被占用时会报错，不会自动换端口。

## 7. 配置落地建议

建议下一阶段把配置分成三层：

1. 平台配置：应用入口、管理员、角色、应用权限，由聚合管理后台维护。
2. 业务配置：会员套餐、权益、邀请活动、支付方式、协议、主题开关，由轻间管理后台维护。
3. 前台展示：拾隅前台只读取配置并展示，不再把价格、权益数量、协议名称等写死在页面里。

会员校验需要同时做前端提示和服务端拦截。前端负责显示会员标识、弹窗和置灰状态；服务端负责真正判断是否可新增、是否可设置密码/有效期、是否可使用更多样式，不能只依赖前端。

会员到期后的建议规则：

- 已创建内容保留。
- 超出免费额度的内容变为只读。
- 免费额度内的内容仍可查看和基础编辑。
- 需要会员的操作继续触发会员弹窗。
- 不建议清空用户历史内容，这会严重损害用户信任。

## 8. 正式化前必须补的部分

当前很多能力仍是本地 Mock 或前端模拟，正式接手时需要补齐：

- 真实用户系统：前台用户身份、后台管理员身份分离。
- 真实会员服务：会员状态、有效期累加、订单、退款、连续订阅、奖励到账。
- 真实支付：支付宝/微信支付、回调验签、订单状态机、失败重试。
- 真实分享：分享数据持久化、密码加密、阅后即焚、访问日志、风控。
- 配置 API：会员套餐、权益、协议、邀请活动、主题和应用入口都要可配置。
- 数据库迁移：每个业务仓库独立维护自己的数据结构和迁移。
- 权限审计：后台操作应有审计日志，平台授权不能只靠菜单隐藏。
- 构建与部署：当前前台是静态原型结构，正式项目需要重新梳理构建、环境变量和部署链路。

## 9. 给接手人的工作顺序

建议先按这个顺序接：

1. 先启动并跑通当前前台：`D:\系统文件\文档\ChatGPT\导航站`，确认 `http://127.0.0.1:4318/` 可访问。
2. 再启动聚合管理后台：`D:\系统文件\文档\ChatGPT\聚合管理后台`，确认 `http://127.0.0.1:5175/` 可登录。
3. 跑通轻时间和轻间两个业务后台，确认从聚合页能进入对应应用。
4. 梳理哪些前台写死配置要迁移到后台，优先迁移会员套餐、价格、协议、权益数量和邀请奖励。
5. 把分享功能服务端化，特别是密码、有效期和“阅后即焚（仅访问一次）”。
6. 最后再整理前台代码结构，把当前原型里的覆盖式脚本逐步拆成可维护模块。

## 10. 交接重点提醒

- 不要误删 `baselines/`，它记录了大量历史版本快照；但也不要默认把它们当作正式源码继续开发。
- 不要把 `.local-shares/` 当作正式分享数据，它只是本地模拟。
- 不要把 PC 管理后台改成业务后台大杂烩；它更像一个“入口 + 权限 + 平台配置”的壳。
- 会员、支付、邀请、主题权益这些规则，前台已经有体验样子，但正式实现应从后台配置和服务端校验开始。
- 分享入口对所有用户开放；只有设置密码、设置有效期、阅后即焚等高级分享能力需要会员。
- 当前前台用户体验改动非常密集，后续每次改动都要按主题和视口回归，不要只看构建通过。
