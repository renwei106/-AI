const {chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1440,height:900}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>window.scrollTo(0,document.documentElement.scrollHeight));await p.mouse.move(1100,450);await p.mouse.wheel(0,180);await p.waitForTimeout(100);await p.mouse.wheel(0,300);await p.waitForTimeout(1000);assert.equal(await p.evaluate(()=>view),'space');assert.equal(await p.evaluate(()=>scrollY),0);const h=await p.locator('.workspace-top').boundingBox();assert(h.y>=0);for(let i=0;i<3;i++)await p.evaluate(()=>renderGroups());assert.equal(await p.locator('.workspace-tools .group-view-controls').count(),1);const search=await p.locator('#filter').boundingBox(),controls=await p.locator('.workspace-tools .group-view-controls').boundingBox();assert(Math.abs(search.y+search.height/2-controls.y-controls.height/2)<3);await p.screenshot({path:'checks/v6/navigation-controls.png'});await p.mouse.wheel(0,-160);await p.waitForTimeout(1200);assert.equal(await p.locator('.peek-return').textContent(),'继续向上滑动返回首页');assert.equal(await p.locator('.peek-return').evaluate(e=>getComputedStyle(e).opacity),'1');await p.mouse.wheel(0,-160);await p.waitForTimeout(700);assert.equal(await p.evaluate(()=>view),'home');assert.deepEqual(errors,[]);await b.close();console.log('PASS scroll position, return gesture, aligned controls, repeated group renders');})().catch(e=>{console.error(e);process.exit(1)});
