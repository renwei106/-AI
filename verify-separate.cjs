const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>goSpace(data[0].id));assert.equal(await p.locator('.group-view-controls>button').count(),2);await p.locator('[data-link-settings]').click();assert.equal(await p.locator('[data-link-view=list]').count(),0);assert.equal(await p.locator('#link-view-settings .link-column-setting').count(),0);await p.locator('[data-link-view=paper]').click();assert.equal(await p.locator('.paper-site-identity .link-emblem').count(),3);await p.locator('#link-view-settings [data-action=close]').first().click();await p.locator('[data-column-settings]').click();await p.locator('[data-separate-columns="2"]').click();assert.equal(await p.locator('.group:not([hidden]) .cards').evaluate(e=>e.style.getPropertyValue('--group-columns')),'2');await b.close();console.log('PASS separate controls, no list, newspaper identity and columns')})().catch(e=>{console.error(e);process.exit(1)});
