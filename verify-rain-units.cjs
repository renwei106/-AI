const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318/');console.log(await p.evaluate(()=>{prefs.theme='rain';apply();render();const result=[];for(const [h,m]of [['03','24'],['04','25'],['10','00']]){updateRainPart(document.querySelector('[data-rain-hour]'),h,'hour');updateRainPart(document.querySelector('[data-rain-minute]'),m,'minute');result.push([...document.querySelectorAll('.rain-value')].map(e=>e.textContent));}const units=[...document.querySelectorAll('.rain-time-unit')].map(e=>{const s=getComputedStyle(e);return{size:s.fontSize,color:s.color,opacity:s.opacity}});return{result,units}}));await b.close()})().catch(e=>{console.error(e);process.exit(1)});
