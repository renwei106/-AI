const fs=require('fs');let s=fs.readFileSync('dist/v4.js','utf8');const old="coordinates.replaceChildren(document.createTextNode(names[planetView]),document.createElement('br'),document.createTextNode(PLANET_POETRY[planetView]));";if(!s.includes(old))throw Error('copy not found');s=s.replace(old,"const [title,...description]=PLANET_POETRY[planetView].split(' · ');coordinates.replaceChildren(...[title,names[planetView],description.join(' · ')].map(line=>{const span=document.createElement('span');span.textContent=line;span.className='planet-copy-line';return span}));");fs.writeFileSync('dist/v4.js',s);
