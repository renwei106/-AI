const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');const fs=require('node:fs');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>goSpace(data[0].id));await p.locator('[data-share-open]').click();await p.locator('[name=scope][value=scene]').check();await p.locator('[name=access][value=password]').check();await p.locator('#random-share-password').click();const password=await p.locator('[name=password]').inputValue();assert.equal(password.length,16);const download=p.waitForEvent('download');await p.locator('#share-form [type=submit]').click();const file=await download;const path='checks/v6/share-encrypted.html';await file.saveAs(path);const html=fs.readFileSync(path,'utf8');assert(!html.includes('Notion'));const q=await b.newPage();await q.goto('file:///'+process.cwd().replaceAll('\\','/')+'/'+path);await q.locator('#unlock input').fill('incorrect');await q.locator('#unlock button').click();await q.waitForTimeout(300);assert.equal(await q.locator('#content a').count(),0);await q.locator('#unlock input').fill(password);await q.locator('#unlock button').click();await q.waitForSelector('#content a');assert.equal(await q.locator('#content h2').count(),1);await b.close();console.log('PASS share download, random password, encryption, wrong-password rejection, scene-only scope')})().catch(e=>{console.error(e);process.exit(1)});
