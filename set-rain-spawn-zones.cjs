const fs=require('fs');let s=fs.readFileSync('dist/v4.js','utf8');const old="mass:el.classList.contains('rain-value')?8:1,x:r+Math.random()*Math.max(1,w-2*r),y:-40";const replacement="mass:el.classList.contains('rain-value')?8:1,x:el.closest('[data-rain-hour]')?w*.25:el.closest('[data-rain-minute]')?w*.75:w*(.44+Math.random()*.12),y:-Math.max(rect.width,rect.height)-40";if(!s.includes(old))throw Error('spawn not found');s=s.replace(old,replacement);fs.writeFileSync('dist/v4.js',s);
