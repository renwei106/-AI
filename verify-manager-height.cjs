const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{signed=true;for(let i=0;i<30;i++)data.push({...structuredClone(data[0]),id:'s'+i,name:'空间'+i});manageOrganization('space')});for(const size of [{width:1440,height:1000},{width:390,height:844}]){await p.setViewportSize(size);const box=await p.locator('#organization').boundingBox();assert(box.height<=Math.min(680,size.height-32)+1);assert(await p.locator('.organization-list').evaluate(e=>e.scrollHeight>e.clientHeight));await p.locator('#organization [data-create=space]').click();assert(await p.locator('.inline-create input').isVisible());await p.locator('.inline-create input').fill('新增'+size.width);await p.locator('.inline-create').dispatchEvent('mouseleave');assert(await p.locator('#organization .dialog-done button').isVisible())}await b.close();console.log('PASS capped manager height, scrollable list, inline creation and visible footer on desktop/mobile')})().catch(e=>{console.error(e);process.exit(1)});
