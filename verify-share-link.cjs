const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>goSpace(data[0].id));await p.locator('[data-share-open]').click();await p.locator('[name=shareSpace]').selectOption('life',{force:true});await p.locator('[name=access][value=password]').check();const password=await p.locator('[name=password]').inputValue();assert.equal(password.length,6);const boxes=await p.locator('.share-selection>label').evaluateAll(es=>es.map(e=>e.getBoundingClientRect().y));assert.equal(boxes[0],boxes[1]);await p.locator('#share-form [type=submit]').click();await p.waitForSelector('#generated-share-link');const link=await p.locator('#generated-share-link').inputValue();assert(!link.includes(password));const q=await b.newPage();await q.goto(link);assert.equal(await q.locator('#content a').count(),0);await q.locator('#unlock input').fill('badpass');await q.locator('#unlock button').click();await q.waitForTimeout(150);assert.equal(await q.locator('#content a').count(),0);await q.locator('#unlock input').fill(password);await q.locator('#unlock button').click();await q.waitForSelector('#content a');assert.equal(await q.locator('#content h1').innerText(),'生活空间');await b.close();console.log('PASS horizontal range, six-character password, link creation, wrong password blocked, correct password allowed')})().catch(e=>{console.error(e);process.exit(1)});
