const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage({viewport:{width:1440,height:900}});await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>goSpace(data[0].id));await p.locator('.heading-space-switch').click();const menu=await p.locator('.heading-space-menu').boundingBox(),sidebar=await p.locator('.sidebar').boundingBox(),content=await p.locator('.workspace-content').boundingBox();assert(Math.abs(menu.width-sidebar.width)<2);assert(menu.x+menu.width<=content.x);await p.screenshot({path:'checks/v6/space-menu-sidebar-width.png'});await b.close();console.log('PASS sidebar width and no content overlap')})().catch(e=>{console.error(e);process.exit(1)});
