const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1440,height:1000}});await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{signed=true;render()});await p.locator('.dock-trigger').hover();await p.waitForTimeout(300);const first=p.locator('.space-strip .space-option').first(),last=p.locator('.space-strip .space-option').last(),a=await first.boundingBox(),z=await last.boundingBox();await p.mouse.move(a.x+a.width/2,a.y+a.height/2);await p.mouse.down();await p.waitForTimeout(440);await p.mouse.move(z.x+z.width-5,z.y+z.height/2,{steps:8});await p.mouse.up();assert.equal(await p.evaluate(()=>data.at(-1).id),'work');await p.locator('[data-organize=space]').click();await p.mouse.click(5,5);assert(!(await p.locator('#organization').isVisible()));await p.reload();assert.equal(await p.evaluate(()=>data.at(-1).id),'work');await p.locator('.dock-trigger').hover();await p.waitForTimeout(300);await p.screenshot({path:'checks/v6/dock-aligned.png'});await b.close();console.log('PASS long press reorder persisted, backdrop close')})().catch(e=>{console.error(e);process.exit(1)});
