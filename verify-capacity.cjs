const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');assert(await p.evaluate(()=>data[0].scenes.length===30&&data[0].scenes.every(c=>c.groups.length===20)));await p.reload();assert.equal(await p.evaluate(()=>data[0].scenes.length),30);await p.evaluate(()=>goSpace(data[0].id));assert.equal(await p.locator('.top-return').count(),1);assert.equal(await p.locator('.top-return').evaluate(e=>getComputedStyle(e).position),'static');await p.evaluate(()=>dock());assert.equal(await p.locator('.top-return').count(),1);await p.evaluate(()=>{view='home';prefs.theme='flip';homeTimerMode='focus';prefs.focusMinutes=120;render()});const r=await p.locator('.focus-duration').evaluate(e=>[...e.children].map(x=>{const r=x.getBoundingClientRect();return r.y+r.height/2}));assert(Math.max(...r)-Math.min(...r)<1);await p.locator('[data-v2=focus-toggle]').click();assert(!(await p.locator('.focus-duration').isVisible()));assert.equal(await p.locator('[data-v2=focus-reset]').evaluate(e=>getComputedStyle(e).borderRadius),'10px');await b.close();console.log('PASS capacity persisted, single top return, input centered, reset style')})().catch(e=>{console.error(e);process.exit(1)});
