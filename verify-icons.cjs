const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{signed=true;goSpace('work')});assert.equal(await p.locator('[data-organize=group] svg').count(),1);for(const kind of ['scene','group']){await p.evaluate(k=>manageOrganization(k),kind);await p.locator('#organization [data-create='+kind+']').click();await p.locator('.inline-create input').fill('图标测试'+kind);await p.locator('.inline-create .entity-icon-button').click();await p.locator('[data-pick-icon=star]').click();assert(await p.locator('.inline-create input').isVisible());await p.locator('.inline-create').dispatchEvent('mouseleave');assert.equal(await p.evaluate(k=>organizationList(k).at(-1).icon,kind),'star');await p.locator('#organization .entity-icon-button').last().click();await p.locator('[data-pick-icon=book]').click();assert.equal(await p.evaluate(k=>organizationList(k).at(-1).icon,kind),'book');await p.locator('#organization [data-action=close]').first().click()}await p.reload();assert(await p.evaluate(()=>data[0].scenes.at(-1).icon==='book'&&data[0].scenes[0].groups.at(-1).icon==='book'));assert.deepEqual(errors,[]);await b.close();console.log('PASS setting icon, draft icon selection, existing editing, return to list, persistence')})().catch(e=>{console.error(e);process.exit(1)});
