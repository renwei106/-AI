const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1440,height:900}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>goSpace(data[0].id));await p.locator('[data-link-settings]').click();for(const id of ['paper','record','film','planet','calendar']){await p.locator('[data-link-view='+id+']').click();assert.equal(await p.locator('.group:not([hidden]) .cards').getAttribute('data-display'),id);assert.equal(await p.locator('.group:not([hidden]) .bookmark a').count(),3);assert.equal(await p.locator('[data-link-columns]').count(),id==='paper'?4:6)}await p.locator('[data-link-view=follow]').click();await p.locator('#link-view-settings [data-action=close]').first().click();await p.evaluate(()=>{prefs.theme='music';render()});assert.equal(await p.locator('.group:not([hidden]) .cards').getAttribute('data-display'),'record');await p.screenshot({path:'checks/v6/record-links.png'});assert.deepEqual(errors,[]);await b.close();console.log('PASS five themed views, dynamic columns, follow-theme and existing links preserved')})().catch(e=>{console.error(e);process.exit(1)});
