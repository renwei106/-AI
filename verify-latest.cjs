const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');for(const n of [99,100,180]){await p.evaluate(n=>{prefs.theme='flip';prefs.focusMinutes=n;focusSeconds=n*60;homeTimerMode='focus';render()},n);assert.equal(await p.locator('.focus-unit-card').count(),n<100?2:3);assert.equal(await p.locator('.focus-unit-card').first().getAttribute('data-value'),n===99?'99':n===100?'01':'03')}await p.evaluate(()=>{prefs.theme='music';render()});assert(!(await p.locator('.player-controls .select-trigger small').isVisible()));await p.locator('[data-v3=audio-play]').click();assert(await p.locator('.equalizer').evaluate(e=>e.classList.contains('moving')));await p.locator('[data-v3=audio-play]').click();assert(!(await p.locator('.equalizer').evaluate(e=>e.classList.contains('moving'))));await p.evaluate(()=>{signed=true;goSpace('work')});const r=await p.locator('[data-scene=daily]').boundingBox();await p.mouse.move(r.x+20,r.y+20);await p.mouse.down();await p.waitForTimeout(450);await p.mouse.move(r.x+40,r.y+30);assert.equal(await p.locator('.drag-visual').count(),1);const g=await p.locator('.drag-visual').boundingBox();assert(Math.abs(g.x-r.x-20)<2);await p.mouse.up();assert.equal(await p.locator('.drag-visual').count(),0);assert.deepEqual(errors,[]);await b.close();console.log('PASS 99/100/180 time blocks, music play/pause, immediate drag ghost')})().catch(e=>{console.error(e);process.exit(1)});
