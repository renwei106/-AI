const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage({viewport:{width:1440,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');assert.equal(await p.locator('.brand-en').count(),0);await p.evaluate(()=>{goSpace(data[0].id);const g=currentGroup();g.items=Array.from({length:235},(_,i)=>['网址'+i,'https://example.com/'+i,'描述','']);renderGroups()});assert.equal(await p.locator('.group:not([hidden]) .bookmark:not(.page-hidden)').count(),100);await p.locator('[data-bookmark-page="3"]').click();assert.equal(await p.locator('.group:not([hidden]) .bookmark:not(.page-hidden)').count(),35);await p.evaluate(()=>window.scrollTo(0,450));await p.waitForTimeout(500);const bar=await p.locator('.group-tab-bar').boundingBox();assert(Math.abs(bar.y)<2);await p.locator('[data-sidebar-collapse]').click();assert(await p.locator('.workspace').evaluate(e=>e.classList.contains('sidebar-collapsed')));await p.screenshot({path:'checks/v6/pagination-collapsed.png'});assert.deepEqual(errors,[]);await b.close();console.log('PASS default100, last page35, sticky group bar, sidebar collapse')})().catch(e=>{console.error(e);process.exit(1)});
