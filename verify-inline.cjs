const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{signed=true;goSpace('work')});for(const kind of ['scene','group','space']){await p.evaluate(k=>manageOrganization(k),kind);for(let i=0;i<2;i++){await p.locator('#organization [data-create='+kind+']').click();assert.equal(await p.locator('#organization-form').count(),0);await p.locator('.inline-create input').fill(kind+i);await p.locator('.inline-create').dispatchEvent('mouseleave');assert(await p.locator('#organization').isVisible());assert((await p.locator('.organization-list').innerText()).includes(kind+i));}await p.locator('#organization [data-action=close]').first().click()}await p.reload();assert(await p.evaluate(()=>data.some(x=>x.name==='space1')&&data[0].scenes.some(x=>x.name==='scene1')&&data[0].scenes[0].groups.some(x=>x.name==='group1')));await b.close();console.log('PASS inline creation for all three types, repeated creation, stays in list, reload persistence')})().catch(e=>{console.error(e);process.exit(1)});
