const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage({viewport:{width:1440,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{signed=true;goSpace(data[0].id);manageOrganization('space')});await p.locator('[data-icon-kind=space]').first().click();assert.equal(await p.locator('.icon-library-tabs button').count(),10);assert.equal(await p.locator('#entity-icon-picker [data-pick-icon]').count(),20);await p.locator('[data-icon-category="自然天气"]').click();await p.screenshot({path:'checks/v6/icon-library.png'});await p.locator('[data-pick-icon]').first().click();assert.equal(await p.locator('.space-detail-icon').count(),1);assert.equal(await p.locator('.dock-options .space-detail-icon').count(),0);assert(await p.locator('#organization').evaluate(e=>e.open));assert.deepEqual(errors,[]);await b.close();console.log('PASS 10 categories, 20 icons per category, space icon persistence and picker return')})().catch(e=>{console.error(e);process.exit(1)});
