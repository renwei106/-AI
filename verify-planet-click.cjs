const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{prefs.theme='cosmos';render()});for(const id of ['earth','mars','saturn','sun']){await p.locator('[data-planet='+id+']').dispatchEvent('click');await p.waitForTimeout(160);assert.equal(await p.locator('.orbit-body.is-center').getAttribute('data-planet'),id);const moving=p.locator('.orbit-body:not(.is-center)').first();const before=await moving.getAttribute('style');const spin=await p.locator('.is-center .planet-surface').getAttribute('style');await p.waitForTimeout(180);assert.notEqual(await moving.getAttribute('style'),before);assert.notEqual(await p.locator('.is-center .planet-surface').getAttribute('style'),spin);assert.equal(await p.locator('.orbit-track').count(),8)}assert.deepEqual(errors,[]);await b.close();console.log('PASS repeated center changes retain orbit motion and self rotation, eight tracks, no errors')})().catch(e=>{console.error(e);process.exit(1)});
