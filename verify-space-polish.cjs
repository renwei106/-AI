const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage({viewport:{width:1440,height:900}});await p.goto('http://127.0.0.1:4318');assert.equal(await p.locator('.space-management button').count(),1);await p.evaluate(()=>goSpace(data[0].id));assert.equal(await p.locator('.space-management button').count(),2);await p.locator('.editable-scene-description').click();assert.equal(await p.evaluate(()=>getSelection().toString()),await p.locator('.editable-scene-description').textContent());await p.locator('.heading-space-switch').click();await p.screenshot({path:'checks/v6/space-menu-polish.png'});await p.evaluate(()=>openLinkSettings());assert.equal(await p.locator('#link-view-settings h2').textContent(),'网址样式');assert((await p.locator('#link-view-settings .view-setting-note').textContent()).includes('分组生效'));await b.close();console.log('PASS footer contexts, select all description, style title and scope hint')})().catch(e=>{console.error(e);process.exit(1)});
