const {chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');let browser;
(async()=>{browser=await chromium.launch({channel:'msedge',headless:true});const p=await browser.newPage({viewport:{width:1381,height:1041}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318/',{waitUntil:'networkidle'});
 await p.evaluate(()=>{prefs.mode='light';prefs.theme='base';render();});await p.locator('.corner-entry').click();await p.waitForTimeout(600);
 await p.evaluate(()=>{const a=prefs.cornerCollections.guest;const g=a.groups.find(x=>x.system==='inbox');Object.assign(g,{name:'旧名称',icon:'lib-Star',iconMode:'theme',color:'rose',sort:'frequency',refs:[{id:'first',url:'https://example.com/first',own:['第一个网址','https://example.com/first','']},{id:'second',url:'https://example.com/second',own:['第二个网址','https://example.com/second','']}]});a.usage={'https://example.com/second':20};persist();});await p.locator('.corner-close-entry').click();await p.locator('.corner-entry').click();
 const inbox=p.locator('.corner-inbox');await inbox.locator('.corner-card-front').evaluate(e=>e.click());await p.waitForTimeout(800);await inbox.locator('.corner-card-front').evaluate(e=>e.click());await p.waitForTimeout(900);
 assert.equal(await inbox.locator('h3').textContent(),'暂存');assert.equal(await inbox.locator('.corner-card-back').count(),0);assert.equal(await inbox.locator('[data-corner-drag-group],[data-corner-drag-ref],[data-corner-sort]').count(),0);assert(!(await inbox.evaluate(e=>e.classList.contains('is-flipped'))));await inbox.locator('.corner-card-front').focus();await p.keyboard.press('Enter');await p.keyboard.press('Space');assert(!(await inbox.evaluate(e=>e.classList.contains('is-flipped'))));
 assert.deepEqual(await inbox.locator('.corner-link a>span').allTextContents(),['第一个网址','第二个网址']);const h=await inbox.locator('.corner-card-front').evaluate(e=>e.offsetHeight),other=await p.locator('.corner-card:not(.corner-inbox):not(.corner-add-card) .corner-card-front').first().evaluate(e=>e.offsetHeight);assert.equal(h,other);
 const initial=await inbox.locator('.corner-card-logo').innerHTML();const palette=()=>inbox.locator('.corner-card-front').evaluate(e=>({bg:getComputedStyle(e).backgroundImage,color:getComputedStyle(e).color}));const initialPalette=await palette();await p.screenshot({path:'checks/fixed-inbox-light.png'});
 for(const theme of ['music','cosmos','flow','paper']){await p.locator('.corner-close-entry').click();await p.evaluate(t=>{prefs.theme=t;render();},theme);await p.locator('.corner-entry').click();await inbox.locator('.corner-card-front').evaluate(e=>e.click());await p.waitForTimeout(500);assert.equal(await inbox.locator('.corner-card-logo').innerHTML(),initial);assert.deepEqual(await palette(),initialPalette);assert.equal(await inbox.locator('.corner-card-front').evaluate(e=>e.offsetHeight),other);}
 const normal=p.locator('.corner-card:not(.corner-inbox):not(.corner-add-card)').first();await normal.locator('.corner-card-front').evaluate(e=>e.click());await p.waitForTimeout(900);await normal.locator('.corner-card-front').evaluate(e=>e.click());await p.waitForTimeout(900);assert(await normal.evaluate(e=>e.classList.contains('is-flipped')));await normal.locator('.corner-back-header').click();await p.waitForTimeout(900);assert(!(await normal.evaluate(e=>e.classList.contains('is-flipped'))));
 await inbox.locator('[data-corner-add-links]').evaluate(e=>e.click());assert(await p.locator('#corner-picker').isVisible());await p.keyboard.press('Escape');await inbox.locator('[data-corner-remove="first"]').evaluate(e=>e.click());assert.equal(await inbox.locator('.corner-link').count(),1);
 await p.locator('.corner-close-entry').click();await p.evaluate(()=>{prefs.mode='dark';render();});await p.locator('.corner-entry').click();await inbox.locator('.corner-card-front').evaluate(e=>e.click());await p.waitForTimeout(900);await p.screenshot({path:'checks/fixed-inbox-dark.png'});assert.equal(await inbox.locator('.corner-card-logo').innerHTML(),initial);assert.equal(await inbox.locator('.corner-card-back').count(),0);assert.deepEqual(errors,[]);console.log('PASS: equal height, fixed inbox identity and palette across themes, front only, no sorting, retained URL actions, normal card editing and dark adaptation');
})().catch(e=>{console.error(e);process.exitCode=1}).finally(()=>browser?.close());
