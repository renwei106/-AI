const {chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const c=await b.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true,reducedMotion:'reduce'});await c.addInitScript(()=>!localStorage.getItem('yiyu-prototype-v1')&&localStorage.setItem('yiyu-prototype-v1',JSON.stringify({prefs:{theme:'flow',cornerCollections:{guest:{groups:[{id:'kept',name:'原有卡片',art:1,refs:[{id:'ref',gid:'tools',url:'https://www.notion.so'}]}]}}}})));const p=await c.newPage();await p.goto('http://127.0.0.1:4318/',{waitUntil:'networkidle'});await p.locator('.corner-entry').tap();assert.equal(await p.locator('[data-corner-card]').count(),3);assert.equal(await p.locator('.corner-card-cover h3').first().innerText(),'原有卡片');assert.equal(await p.locator('.corner-link').count(),1);const a=await p.locator('.corner-add-card').boundingBox(),z=await p.locator('.corner-close-entry').boundingBox();assert(a.height>300,'add entry is a full-sized card');await p.screenshot({path:'checks/corner-stage-mobile.png'});await p.locator('.corner-card-cover').first().tap();await p.locator('[data-corner-delete]').first().tap();await p.locator('[data-corner-confirm-delete]').first().tap();await p.locator('.corner-close-entry').tap();await p.reload({waitUntil:'networkidle'});await p.locator('.corner-entry').tap();assert.equal(await p.locator('[data-corner-card]').count(),2,'deleted default cards do not return');await b.close();console.log('PASS: prior card data preserved; default cards initialized once; deletion persists; mobile controls separated.');})().catch(e=>{console.error(e);process.exit(1)});
