const fs=require('fs');let s=fs.readFileSync('dist/v4.js','utf8');s=s.replace('prev.disabled=index===0;next.disabled=index===features.length-1;','prev.disabled=false;next.disabled=false;');s=s.replace('index=Math.max(0,Math.min(features.length-1,index+step));','index=(index+step+features.length)%features.length;');const start=s.indexOf('// Grouped membership feature previews');s=s.slice(0,start)+s.slice(start).replace('去发现，更多会员可能','让喜欢，再多一点余地');fs.writeFileSync('dist/v4.js',s);
