const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage();await p.goto('http://127.0.0.1:4318');for(const mode of ['light','dark','system']){await p.evaluate(m=>{prefs.mode=m;render()},mode);await p.locator('.brand').click();assert.equal(await p.evaluate(()=>prefs.mode),mode);await p.locator('[data-action=settings]').click();for(const theme of ['music','paper','cosmos','base']){await p.locator('[data-v2-theme="'+theme+'"]').click();assert.equal(await p.evaluate(()=>prefs.mode),mode)}await p.locator('#settings .dialog-done button').click();await p.reload();assert.equal(await p.evaluate(()=>prefs.mode),mode)}await b.close();console.log('PASS all three modes retained through brand, theme gallery and reload')})().catch(e=>{console.error(e);process.exit(1)});
