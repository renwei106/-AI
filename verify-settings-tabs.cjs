const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage({viewport:{width:1440,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{settingsTab='layout';renderSettings();document.querySelector('#settings').showModal()});assert.equal(await p.locator('#settings .layout-card').count(),2);assert.equal(await p.locator('#settings .dialog-done').count(),1);await p.screenshot({path:'checks/v6/settings-layout-tabs.png'});await p.locator('[data-settings-tab=copy]').click();await p.locator('[data-shared-copy-mode=custom]').click();await p.locator('[data-shared-copy-field=title]').fill('共同的灵感');assert.equal(await p.evaluate(()=>currentCopy('music').title),'共同的灵感');assert.equal(await p.evaluate(()=>currentCopy('cinema').title),'共同的灵感');await p.evaluate(()=>{document.querySelector('#settings').close();goSpace(data[0].id);scopeDialog()});assert.equal(await p.locator('[data-space-style]').count(),0);await p.locator('[data-space-settings-tab=font]').click();assert(await p.getByText('自动跟随全局设置').count());await p.locator('[data-space-settings-tab=layout]').click();assert.equal(await p.locator('#display-scope-dialog .layout-card:disabled').count(),1);await p.screenshot({path:'checks/v6/space-tabs-layout.png'});assert.deepEqual(errors,[]);await b.close();console.log('PASS layout diagrams, single footer, shared copy, space tabs and locked previews')})().catch(e=>{console.error(e);process.exit(1)});
