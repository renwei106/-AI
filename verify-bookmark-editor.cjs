const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{signed=true;goSpace(data[0].id)});await p.locator('[data-link-settings]').click();assert.equal(await p.locator('.more-link-views [data-link-view=icons]').count(),1);assert.equal(await p.locator('.follow-priority').count(),1);await p.locator('[data-link-view=calendar]').click();await p.locator('#link-view-settings [data-action=close]').first().click();assert.equal(await p.locator('.link-calendar small').first().innerText(),'01 日');await p.locator('.group:not([hidden]) .bookmark').first().hover();await p.locator('[data-bookmark-edit="0"]').click();await p.locator('#bookmark-editor-form [name=name]').fill('新的收藏');await p.locator('#bookmark-editor-form [name=url]').fill('https://example.com/new');await p.locator('#bookmark-editor-form [name=logo]').fill('');await p.locator('#bookmark-editor-form [name=description]').fill('新的备注');await p.locator('#bookmark-editor-form .primary').click();assert.equal(await p.locator('.link-calendar .link-emblem').first().innerText(),'新');assert.equal(await p.locator('.group:not([hidden]) .bookmark a').first().getAttribute('target'),'_blank');await p.reload();assert.equal(await p.evaluate(()=>data[0].scenes[0].groups[0].items[0][2]),'新的备注');await b.close();console.log('PASS section grouping, calendar numbers, editing fields and fallback, new-tab target, persistence')})().catch(e=>{console.error(e);process.exit(1)});
