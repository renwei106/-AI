const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{goSpace(data[0].id);styles[currentGroup().id]='calendar';renderGroups()});assert.deepEqual(await p.locator('.link-calendar>small').allTextContents(),['1','2','3']);assert.equal(await p.locator('.group:not([hidden]) .link-name').count(),0);assert.equal(await p.locator('.calendar-description').first().innerText(),await p.evaluate(()=>currentGroup().items[0][2]));assert.equal(await p.locator('.link-calendar .link-emblem').count(),3);await b.close();console.log('PASS plain numbers, no duplicate names, description and logo inside calendar')})().catch(e=>{console.error(e);process.exit(1)});
