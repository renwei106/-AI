const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage({viewport:{width:2068,height:1041}});await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{prefs.theme='cosmos';prefs.mode='dark';render();settingsTab='themes';renderSettings();document.querySelector('#settings').showModal()});await p.locator('.swatch').first().click();await p.locator('.swatches').scrollIntoViewIfNeeded();const r=await p.locator('.swatches').boundingBox(),dot=await p.locator('.swatch').first().boundingBox();assert(dot.x-r.x>=5);assert(r.y+r.height-dot.y-dot.height>=5);await p.screenshot({path:'checks/v6/swatches-unclipped.png'});await b.close();console.log('PASS swatch outline fits inside scroll content')})().catch(e=>{console.error(e);process.exit(1)});
