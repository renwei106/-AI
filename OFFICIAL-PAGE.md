# 拾隅官网 · 第一版

## 范围

新增独立单页官网，保留原产品页面、导航、登录和数据逻辑。本次只新增 `dist/official/`、`official-preview.cjs`、本说明和 `checks/official-v1/`。当前产品文件原有未提交变更未纳入本次修改。

品牌统一为「拾隅」。页面以「世界很大，留一隅给自己」开篇，依次介绍收藏整理、搜索找回、平台发现、个性首页，最后邀请进入产品。

## 本地预览

```powershell
node official-preview.cjs
```

访问 http://127.0.0.1:4320/official/ 。服务只监听本机。产品入口通过该服务的 `/` 跳转到原产品 http://127.0.0.1:4318/，原产品预览服务需要保持运行。

独立服务避免重新启动或修改正在使用的产品预览。官网文件使用相对资源路径；后续可以把整个 `official` 目录挂载在产品域名的 `/official/`。网站代码中的进入产品链接是 `/`。

## 视觉与滚动

- 暖白、苔绿、深墨绿，宋体大标题与简洁正文。
- 首屏海景随滚动从画框扩展至全屏，叙事文字淡入。
- 收藏空间在停驻章节中缓慢展开，三步介绍随进度切换。
- 探索章节在桌面停驻并横向展开，移动端使用原生横向滑动。
- 浏览器原生滚动保持可用，没有拦截滚轮，也没有强制翻屏。
- 主题区支持六种实际首页截图切换、方向键操作和放大查看；手机展示对应的真实手机界面。
- 支持减少动态效果偏好、键盘焦点、关闭大图后恢复焦点；无脚本时主要介绍仍可阅读。

视觉研究参考：

- [Apple AirPods Pro](https://www.apple.com/airpods-pro/)：分章节的大幅产品展示。
- [Apple MacBook Air](https://www.apple.com/macbook-air/)：真实产品画面与简短说明的组合。

上述页面只作为叙事与布局参考；本页文案、样式和交互独立实现，未复制其素材或源码。

## 素材与演示范围

产品图片通过独立的无痕 Playwright 浏览器从现有前台截取，不读取或修改用户浏览器资料。收藏空间截图在该临时浏览器中精简示例数据；风景来自项目已有的原创海报。品牌图标来自现有 `assets/site-icons`。

搜索区提供五项示例收藏的即时检索，网页搜索在新标签页打开必应。平台发现链接会访问标明的对应平台。主题预览是实际界面截图；原产品互动通过「进入拾隅」体验。

本版未宣传自动采集浏览器历史、全网内容保存、云同步或公开社区等未经确认可用的能力。

## 验证

```powershell
node --check dist/official/official.js
node --check official-preview.cjs
node checks/official-v1/verify.cjs
```

检查覆盖桌面和手机视口、资源加载、搜索匹配及空态、网页搜索入口、主题切换、键盘方向键、大图与 Esc、焦点恢复、章节导航、产品入口、原生横向图库、减少动态效果和无脚本阅读。截图与结果存放在 `checks/official-v1/`。

无需安装新的前端依赖。`capture-product.cjs` 和 `capture-mobile.cjs` 可重新采集产品图片；它们依赖本机现有 Playwright 和 Sharp 运行时。
