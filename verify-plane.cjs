const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1440,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{prefs.theme='cosmos';render()});const tracks=await p.locator('.orbit-track').evaluateAll(es=>es.map(e=>e.style.transform));assert.equal(tracks.length,8);const bases=tracks.map(t=>t.match(/matrix\(([^)]+)/)[1].split(',').map(Number));for(const v of bases){assert(Math.abs(v[1]/v[0]-bases[0][1]/bases[0][0])<.00001);assert(Math.abs(v[3]/v[0]-bases[0][3]/bases[0][0])<.00001)}const before=await p.locator('[data-planet=earth]').getAttribute('style');await p.waitForTimeout(500);assert.notEqual(await p.locator('[data-planet=earth]').getAttribute('style'),before);const c=await p.locator('.is-center').boundingBox();await p.mouse.move(c.x+100,c.y+100);await p.mouse.down();await p.mouse.move(c.x+220,c.y+120,{steps:8});await p.mouse.up();assert.deepEqual(await p.locator('.orbit-track').evaluateAll(es=>es.map(e=>e.style.transform)),tracks);await p.screenshot({path:'checks/v6/coplanar-orbits.png'});assert.deepEqual(errors,[]);await b.close();console.log('PASS: eight coplanar tracks, automatic movement, fixed view during drag, no errors')})().catch(e=>{console.error(e);process.exit(1)});
