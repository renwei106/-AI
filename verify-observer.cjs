const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1440,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{prefs.theme='cosmos';render()});await p.waitForTimeout(500);const box=await p.locator('.cosmos-copy').boundingBox();await p.locator('[data-planet=earth]').dispatchEvent('click');await p.waitForTimeout(1100);assert(await p.locator('body').evaluate(e=>e.classList.contains('planet-observer')));assert.equal(await p.locator('.is-center').getAttribute('data-planet'),'sun');assert.deepEqual(await p.locator('.cosmos-copy').boundingBox(),box);const before=await p.locator('.is-center .planet-surface').getAttribute('style');await p.waitForTimeout(200);assert.notEqual(await p.locator('.is-center .planet-surface').getAttribute('style'),before);await p.screenshot({path:'checks/v6/observer.png'});await p.locator('[data-planet=earth]').dispatchEvent('click');assert(!(await p.locator('body').evaluate(e=>e.classList.contains('planet-observer'))));await p.waitForTimeout(1100);await p.screenshot({path:'checks/v6/spaced-orbits.png'});assert.deepEqual(errors,[]);await b.close();console.log('PASS observer toggle, unchanged layout/center, central spin, no errors')})().catch(e=>{console.error(e);process.exit(1)});
