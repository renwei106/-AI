const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage({viewport:{width:1440,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{prefs.theme='cosmos';render()});await p.waitForTimeout(300);assert.equal(await p.locator('.orbit-track').count(),8);const before=await p.locator('.orbit-track').first().getAttribute('style');const c=await p.locator('.is-center').boundingBox();await p.mouse.move(c.x+c.width/2,c.y+c.height/2);await p.mouse.down();await p.mouse.move(c.x+c.width/2+100,c.y+c.height/2+20,{steps:10});assert.notEqual(await p.locator('.orbit-track').first().getAttribute('style'),before);await p.mouse.up();assert.equal(new Set(await p.locator('.orbit-body .planet-surface').evaluateAll(es=>es.map(e=>e.style.transform))).size,9);await p.waitForTimeout(2500);await p.screenshot({path:'checks/v6/independent-orbits.png'});await p.setViewportSize({width:390,height:844});await p.screenshot({path:'checks/v6/independent-orbits-mobile.png'});assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));assert.deepEqual(errors,[]);await b.close();console.log('PASS independent tracks/axes, whole-system drag, mobile, no errors')})().catch(e=>{console.error(e);process.exit(1)});
