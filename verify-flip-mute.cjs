const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{prefs.theme='music';render()});assert(await p.locator('.mute-control').evaluate(e=>e.classList.contains('is-audible')));await p.locator('[data-v3=audio-mute]').click();assert.equal(await p.evaluate(()=>muted),true);assert.equal(await p.evaluate(()=>playing),false);await p.locator('[data-v3=audio-mute]').click();assert.equal(await p.evaluate(()=>muted),false);await p.evaluate(()=>{prefs.theme='flip';homeTimerMode='focus';prefs.focusMinutes=100;focusSeconds=6000;render()});assert.equal(await p.locator('.focus-unit-face>.flip-colon').count(),2);await p.evaluate(()=>{focusSeconds=5999;tick()});assert.equal(await p.locator('.focus-unit-card').last().getAttribute('data-value'),'59');assert.equal(await p.locator('.focus-unit-card').last().locator('.flip-old-bottom').innerText(),'00');await p.waitForTimeout(550);assert.equal(await p.locator('.focus-unit-card').last().locator('.flip-old-bottom').evaluate(e=>getComputedStyle(e).opacity),'0');await p.evaluate(()=>{homeTimerMode='date';home()});assert.equal(await p.locator('.digit>.flip-old-bottom').count(),4);assert.equal(await p.locator('.second-card>.flip-old-bottom').count(),1);assert.deepEqual(errors,[]);await b.close();console.log('PASS independent mute, focus colons, old lower half retained then hidden for synchronized flip')})().catch(e=>{console.error(e);process.exit(1)});
