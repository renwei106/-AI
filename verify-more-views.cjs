const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{goSpace(data[0].id);currentGroup().items[0][3]=''});for(const mode of ['cards','paper','record','film','planet','calendar','cloud','poker','book','note','icons']){await p.evaluate(mode=>{styles[currentGroup().id]=mode;renderGroups()},mode);assert.equal(await p.locator('.group:not([hidden]) .bookmark a').count(),3);assert.equal(await p.locator('.group:not([hidden]) .bookmark a').first().locator('.link-emblem,.site-icon').innerText(),'N')}await p.locator('[data-column-settings]').click();assert.equal(await p.locator('.column-options small').count(),0);assert.deepEqual(errors,[]);await b.close();console.log('PASS all eleven styles have logo fallback, new views, number-only choices')})().catch(e=>{console.error(e);process.exit(1)});
