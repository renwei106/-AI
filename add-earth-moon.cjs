const fs=require('fs');let s=fs.readFileSync('dist/v4.js','utf8');s=s.replace("description.join(' · ')].map(line=>", "description.join(' · ').replace(/[。．.]+$/u,'')].map(line=>");const anchor="   el.classList.toggle('is-center',central);el.dataset.solar=p.id;";if(!s.includes(anchor))throw Error('anchor');s=s.replace(anchor,`   if(p.id==='earth'){let moon=el.querySelector('.earth-moon');if(!moon){moon=document.createElement('span');moon.className='earth-moon';moon.setAttribute('aria-hidden','true');el.append(moon)}const angle=reduced?-.8:now/1000*.55,offset=project(Math.cos(angle)*25,Math.sin(angle)*25,0);moon.style.transform='translate('+offset.x+'px,'+offset.y+'px)';moon.style.opacity=offset.depth<0?'.65':'1'}
`+anchor);fs.writeFileSync('dist/v4.js',s);
