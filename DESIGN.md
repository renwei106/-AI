# 一隅 · 第一轮产品与交互设计

## 第五轮：全屏、字体与太阳系

修改前源文件保存到 baselines/v4。本轮仅涉及指定入口、字体预览、主题名称、空间浮层和宇宙视觉交互。

- 右上角增加四角展开图标，使用浏览器 Fullscreen API；Esc 退出。全屏静置 4 秒淡出工具入口，鼠标移动或键盘操作恢复。这是浏览器全屏欣赏，不是操作系统屏保/桌面壁纸安装。
- 明暗入口循环：日间 → 夜间 → 跟随系统。跟随系统使用半明半暗圆图标，并响应系统配色变化。
- 六类字体：黑体、宋体、楷体、仿宋、几何黑体、等宽字体；使用本地字体与跨平台回退。选择实时生效和持久化，完成仅关闭；字体和宽度示意分别标记为非操作区。
- 基础主题改为留白风，翻页时钟与数字雨显示为翻页风、数字风。左上角品牌单击循环下一主题，不弹出选择器。
- 七种主题空间入口均有物象和悬停/键盘反馈：小火车、报纸、唱片、海报、太阳系行星、小翻页牌、数字落雨。宇宙空间依次映射八大行星外观，名称仍对应真实空间，不虚增空间。
- 宇宙中心默认为柔和太阳，八大行星环绕。地球大陆、木星条带、土星环、水星陨坑等纹理用于区分。为界面观感简化为共享椭圆，尺寸和运行速度不按天文比例。横向拖动改变经度/环绕相位，轨道倾角固定，禁止翻倒。

新增 verify-v5.cjs 检查全屏退出和静置恢复、三态与系统响应、字体即时生效、品牌切换、九个天体、固定倾角拖动、七种空间浮层及手机宽度。

## 第四轮：个性化与主题交互精修

调整前版本已保存为本地 Git 提交 75fe46e。远程仓库尚未配置。

- 右上角仅保留日夜切换、个性化设置。设置分为主题与配色、字体、布局三个标签页；颜色联动背景/容器/强调色，深色染色保持较低亮度。弹窗加入完成关闭入口。
- 下拉控件统一为自定义轻量列表框，保留原生 select 数据源，支持键盘选择与 Esc 收起。
- 音乐静音键改为纯图标，声音、唱片、进度与律动统一播放/暂停。暂停进度按 AudioContext 时间保持。
- 宇宙卫星和轨道线共享椭圆。拖动主星可旋转全组，松开后 900ms 开始缓慢恢复默认倾角；移除文字提示。
- 三款原创摄影底图与海报排版：海风来信、未眠之城、一路向远；首页按钮切换，底部空间浮层分别采用电影封面、唱片或星球。
- 数字雨增加年月日落位及字号。报纸扩展版面，期号按 ISO 周历计算（通常一年52期，个别年份53期），作者统一取当前账号名称，无账号时显示“我”。
- 翻页时钟增加小秒数，旧上半翻出、新下半翻入；专注未开始只显示开始，开始后显示暂停/重置，辅助文案居中。空间图标上移，滚动提示在底部。

海报素材生成方法、完整提示词与参考来源见 POSTER-ASSETS.md。仍为本地交互原型；真实用户名称在接入账号后读取 profile.name/user.name。

## 第三轮：四种主题的定向调整（2026-09-12）

本轮基线在 baselines/v2。调整仅涉及电影、音乐、宇宙和数字雨的首页表达与所需交互。

- 电影：封面铺满视口，导航工具叠放在海报上，默认中宋/宋体海报大字，保留实时上映时间、搜索与空间入口。
- 数字雨：秒数桌面 31px、手机 24px；时与分分别比较上次值，首次或变化时才落下；旧数值在新数字落位时向下压缩消失。修改颜色或重新渲染相同时间不会重复落下。
- 宇宙：保持原有文案、时间和观测区域位置。六颗柔和的虚构星球，一颗居中、五颗环绕；点击或键盘激活可交换中心，原中心缩小加入环绕。尊重减少动态效果设置。
- 音乐：移除唱片外框，保留裸唱片、中心轴与刻度，增加带落针/抬针动作的唱臂。内置三段程序合成原创轻音乐：晨雾（松弛）、微光（专注）、晚风（放空）。单个播放/暂停键、曲目选择器和静音键；不自动播放、不依赖外部曲库。当前实现为循环器乐小品，非商业歌曲。

新增检查覆盖：全屏海报边界、实际音频信号与静音、暂停、曲目切换、星球位移与中心交换、数字雨分时独立变化、桌面与手机画面。后续保留前两轮设计记录。

## 第二轮：主题与封面交互（2026-09-12）

已确认基础视觉方向。本轮仅调整首页/空间转场、主题风格与主题对应的首页内容，保留收藏、登录演示和数据结构。第一轮源文件保存在 baselines/v1。

- 顶部首页/空间导航移除。首页滚动到底后继续向下进入空间；空间到顶后向上滑动触发提示，停顿至少 350ms、在 2.6 秒内再滑一次返回。转场 850ms 冷却，触控板连续惯性按一次手势计算。长页面正常滚动，弹窗与输入控件不触发切页。保留底部点击、品牌返回和 PageUp/PageDown 键盘路径。
- 主题选择器包含七种可预览风格：基础、报纸、音乐、电影、宇宙、翻页时钟、数字雨。每种主题有默认明暗、主色与字体，切换回该主题时恢复上次个性设置。全局主题延续到空间，场景逐字段覆盖保持有效。
- 音乐：唱片动效、播放/暂停动效、心情切换、实时日期时间；不播放音频，界面注明无声陪伴。
- 电影：原创 SVG 风景海报，实时上映时间和入场券；不是实际上映资讯。
- 报纸：报头、日期、当前时间、原创黑白插图、来自现有收藏的条目和空间索引。
- 宇宙：轨道、星球、当地时间与日期，使用代码绘制，无远程图片依赖。
- 翻页时钟：四位分隔数字，变位时翻页；切换当前时间/25 分钟专注，可开始、暂停、重置。专注以时间戳计算，在页面保持打开时跨视图继续，不跨刷新保存。
- 数字雨：日期与大时间落位，每秒增加一枚数字，同一分钟内保留，分钟切换时清场并替换分钟；后台恢复时使用真实时间。参考站连接失败，依据用户截图和描述设计，未声称复刻原站。

桌面 1440 像素和手机 390 像素已检查各主题截图，检查范围包含主题传播、独立场景、上下滑动、音乐控制、专注计时、收藏登录衔接与刷新持久化。当前仍为本机交互原型，无真实账号和云端同步。

以下为第一轮设计记录，具体已完成范围以本节为准。

“一隅”为本轮原型暂用名，尚未作为正式品牌确定。

## 范围与基线

2026-09-12：目录初始只有 Git 元数据，无现有应用可覆盖。本轮交付可交互设计原型，验证基础首页、收藏空间、外观设置和收藏时登录的连续体验。未接入真实认证、云端数据库，不作为正式产品上线。

## 视觉方向

暖白底色、墨色文字、苔绿强调色。留白和字号建立层级，工作区采用低对比容器。趣味集中在固定位置的火车入口，悬停、键盘聚焦、点击都能展开。报纸主题改变字形、栏目线与入口物象，保持相同导航含义。移动端将场景列表调整为横向按钮。

## 信息架构

用户 → 空间 → 场景 → 分组 → 收藏。首页搜索区区分网页与本人收藏；空间页内搜索限制为当前场景。空间切换后默认进入该空间第一个场景。收藏搜索可检索全空间并显示所属路径。

## 已实现原型路径

1. 首页 → 底部小火车展开 → 工作/生活/灵感空间 → 场景 → 分组 → 打开网址。
2. 收藏网址 → 填网址、名称、目录 → 保存 → 体验登录 → 自动保存先前填写内容。
3. 全局外观 → 主题、首页样式、明暗、配色、字体、宽度 → 即时预览。
4. 场景外观 → 按字段覆盖全局设置 → 恢复跟随全局。
5. 分组 → 卡片/列表切换，内容和顺序保持。

## 交互规则

所有外链在新标签打开。搜索框支持 / 快捷聚焦。弹窗使用原生 dialog，支持焦点约束和 Esc 关闭。网址仅允许 http/https。未登录新增时保留草稿。登录明确标注本机体验身份，避免误认为真实账号。页面遵循系统减少动态效果偏好。

原型可以自由试用外观和展示设置，并保存在本机。这不是正式版的账号设置存储策略。原型删除为确认后移除，正式版应加入回收站与撤销；原型未实现空间、场景与分组的创建、编辑、拖拽及批量操作。

## 正式架构建议

先采用单应用模块化结构：账号、收藏、搜索、主题解析、首页模板、分组展示和资源获取。不要先拆微服务。

关系型模型：users；spaces(user_id, position)；scenes(space_id, position)；groups(scene_id, position)；bookmarks(group_id, url, title, description, position)；tags 与 bookmark_tags。外观使用 user_preferences、scene_overrides 和 group_display_preferences。每个实体使用稳定 ID、创建与更新时间；可删除内容使用 deleted_at 支持回收站。

后端逐层验证资源所有权，不能信任客户端传入的用户 ID。跨设备保存需要服务端持久化，并显式处理保存中、失败重试和更新冲突。收藏标题与备注始终转义，服务端抓取网站元数据时校验 URL、重定向和内网地址，避免任意网络请求。

主题解析：系统默认 → 全局设置 → 当前场景逐字段覆盖。分组展示单独解析。首页模板与主题分离。未来主题通过能力声明列出可用配色、布局和展示样式。

## 后续设计顺序

先确认本轮视觉与四层导航，再完善新增/编辑空间、场景与分组、移动收藏、待整理和回收站。其后接入真实认证与云端持久化，再专项设计数字雨、电影、音乐主题。当前时钟是布局原型，显示实时小时与分钟，真实翻页运动尚未细化。

## 原型运行

### 本轮文案与封面调整

个性化新增「文案」分类，七种首页风格分别选择默认或自定义主标题、简介。主标题最多 24 字、2 行，简介最多 60 字、3 行，支持换行、中文输入法与即时保存；恢复默认保留自定义草稿。原型依然仅保存在当前浏览器。

报纸使用全宽多栏版面，补充来自现有空间的收藏栏目；移动端按纵向阅读排列。电影空间入口使用海报照片、大标题和深色文字底衬。中心星球支持左右或斜向拖动自转，保持轨道倾角不变。修改前基线保存在 baselines/v5。

使用 Node.js 执行 `node preview.cjs`，打开 http://127.0.0.1:4318 。数据保存在当前浏览器的 yiyu-prototype-v1 键。原型无外部前端依赖，不自动发布。
