const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:4318');const stats=()=>p.evaluate(()=>{const s=data.find(x=>x.name==='灵感空间').scenes.filter(x=>x.id.startsWith('mock-inspiration'));return[s.length,s.flatMap(x=>x.groups).length,s.flatMap(x=>x.groups).reduce((n,g)=>n+g.items.length,0)]});assert.deepEqual(await stats(),[5,27,810]);await p.reload();assert.deepEqual(await stats(),[5,27,810]);await p.evaluate(()=>{prefs.theme='music';render()});assert.equal(await p.locator('.note-slash').count(),1);await p.locator('[data-v3=audio-play]').click();assert(await p.locator('.mute-control').evaluate(e=>e.classList.contains('is-audible')));assert(await p.locator('.equalizer').evaluate(e=>e.classList.contains('moving')));await p.locator('[data-v3=audio-play]').click();assert.equal(await p.locator('.note-slash').count(),1);assert(!(await p.locator('.equalizer').evaluate(e=>e.classList.contains('moving'))));const bottoms=await p.locator('.equalizer i').evaluateAll(es=>es.map(e=>e.getBoundingClientRect().bottom));assert(Math.max(...bottoms)-Math.min(...bottoms)<1);assert.deepEqual(errors,[]);await b.close();console.log('PASS 5 scenes/27 groups/810 bookmarks, idempotent reload, music status and aligned bars')})().catch(e=>{console.error(e);process.exit(1)});
