# 会员体系接入说明

当前版本为本地界面与交互原型，不作为真实鉴权依据。会员状态必须由服务端获取；前端 prefs.membership 不可信。

已搭建：权益横向提示及定位、半月/月/年套餐、免费/会员对比、订单预览、邀请活动及明细空态、个人中心入口、部分新增额度与分享入口校验。

配置：免费 1 空间 / 每空间 3 场景 / 每场景 3 分组 / 每分组 30 网址。会员 20/20/20/300，300 为建议待确认。所有套餐权益一致。价格及奖励待公布，支付不可提交。

上线前必须完成：
- 服务端统一 entitlement 校验覆盖所有创建、修改、拖动、导入、分享、设置接口；包括新建网址切换目标分组后的保存校验。
- 用稳定的 createdAt/ID 决定有效空间、场景和分组，不能依赖当前排序。现阶段仅本地顺序预览，不迁移用户内容。
- 到期保留全部数据，免费额度外只读；允许删除、导出；续费恢复设置。已有独立配置需要在 effective 层统一回退，当前尚未完整接入。
- 订单、支付回调验签及幂等、退款/关闭订单状态、服务端到期时间。
- 邀请 token、有效注册定义、反重复奖励、奖励账本、使用限制；金额、是否可提现需确认。空态不可伪造奖励。
- 当前登录为本地体验，不能把本地会员标记或链接参数当作真实账户授权。
