const{chromium}=require('C:/Users/任伟的机械革命/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');(async()=>{const b=await chromium.launch({channel:'msedge',headless:true});const p=await b.newPage();await p.goto('http://127.0.0.1:4318');await p.evaluate(()=>{prefs.theme='paper';for(let i=0;i<10;i++)data.push({...structuredClone(data[0]),id:'more'+i});render()});for(const size of [{width:1440,height:900},{width:1366,height:768}]){await p.setViewportSize(size);assert(await p.evaluate(()=>document.documentElement.scrollHeight<=innerHeight+1));assert.equal(await p.locator('.paper-index>button').count(),3);assert.equal(await p.locator('.paper-more>article').count(),3);await p.screenshot({path:'checks/v6/paper-'+size.height+'.png'})}await p.evaluate(()=>{prefs.theme='flip';homeTimerMode='focus';render()});assert(await p.locator('.focus-duration').isVisible());await p.locator('[data-v2=focus-toggle]').click();assert(!(await p.locator('.focus-duration').isVisible()));await p.locator('[data-v2=focus-toggle]').click();assert(!(await p.locator('.focus-duration').isVisible()));await p.locator('[data-v2=focus-reset]').click();assert(await p.locator('.focus-duration').isVisible());await b.close();console.log('PASS desktop one-screen newspaper with capped data; focus input hidden until reset')})().catch(e=>{console.error(e);process.exit(1)});
