# 拾隅官网 · 沉浸叙事第二版

预览：[第二版](http://127.0.0.1:4320/official/v2/index.html) · [第一版](http://127.0.0.1:4320/official/)

## 本次范围

只改动第二版 `dist/official/v2/index.html`、`official.css`、`official.js`，新增原创背景、验证脚本与说明。第一版源文件和素材用 SHA-256 与 `baselines/official-v1-20260915` 逐一核对。中断时的第二版快照保存在 `baselines/official-v2-in-progress-20260916`。

## 浏览方式与叙事

进入即显示首屏，向下滚动即可浏览。固定全屏舞台、居中文案、原生滚动驱动连续进度，无点击启动或强制翻屏。向上滚动可以回看。顶部标识可回到开篇，末尾「进入拾隅」连接原产品。

五幕依次表达：

1. **一点喜欢**：暗色场景中的一点暖光，随滚动打开。
2. **给自己**：雾中景观逐渐显露，微光展开成流动的形态。
3. **慢慢成为你**：同一组微光聚拢，海边、道路和雨夜影像柔和浮现。
4. **向内**：微光化成水纹，视觉回到安静的海面与内心。
5. **从容一点**：暗色中出现暖光，画面收束，呈现产品入口。

文案只讲理念与感受。画面采用墨黑、暗松绿、雾灰、琥珀和暖白，视觉焦点与文字沿中轴排列。

## 实现

- 单个 WebGL 点阵在五种形态之间连续插值；粒子保留对应关系，随进度改变位置与色温。
- 风景图缩放、记忆影像、光束和文字由同一条滚动时间轴控制。
- 采用原生滚动与约 135 毫秒的进度缓动，保留滚轮、触摸、Home/End 和 PageUp/PageDown 操作。
- 手机减少粒子数量和像素倍率；切换到后台暂停绘制。
- 遵循系统「减少动态效果」，无环境循环、缩放或模糊动画。
- WebGL 不可用时使用轻量 Canvas 视觉；无 JavaScript 时显示品牌说明及产品入口。
- 无新增前端依赖。原生 HTML/CSS/JS 可由现有 `official-preview.cjs` 提供预览。

视觉参考：[Lusion About](https://lusion.co/about)，借鉴固定视野、持续演变和文字出场节奏。当前实现由实时点阵及分层图像构成。

## 素材

原创场景使用内置 image_gen 生成，原图与实际使用的 WebP 均已保存在第二版 assets 下。完整提示词与文件链接见 [image-prompt.md](checks/official-v2/image-prompt.md)。

## 验证

```powershell
node --check dist/official/v2/official.js
node checks/official-v2/verify-scroll.cjs
node checks/official-v2/verify-finish.cjs
```

结果与截图位于 `checks/official-v2/scroll-review/`。验证覆盖：

- 1440 × 900 桌面及 390 × 844 手机的五幕构图；
- 无点击开场、滚轮推进及回看、固定视口；
- 手机原生触摸滑动、键盘操作及最终产品入口；
- 无横向溢出、标题在视口内、图片与脚本正常加载；
- 减少动态效果下静止截图一致；
- WebGL 不可用和禁用 JavaScript 时的入口；
- 第一版文件及素材保持一致。
- 补查 320 × 568 窄屏、844 × 390 横屏，五幕内容和入口均完整显示；
- 检查章节过渡时最多显示一段主要文案，避免标题重叠。

预览仍为本机服务，未对外发布。产品入口通过 4320 服务的根路径跳到现有 4318 产品预览。

## 当前版本存档

2026-09-16 将当前第二版完整复制到 `baselines/official-v2-20260916/`，共 37 个页面与素材文件，逐文件 SHA-256 核对一致。该快照、第二版源代码、本说明、素材提示词及验证脚本/JSON 记录一起提交到 Git。
